# synced from apix-actions/.github/workflows/issue-closed.yaml
name: Close Jira Ticket on Issue Close

on:
  issues:
    types: [closed]

permissions: {}

jobs:
  close-jira:
    permissions:
      contents: read
      issues: read
    uses: mongodb/apix-action/.github/workflows/_issue-closed.yaml@8c5007247643059c0d15c061290a345eb987657f # main
    secrets: inherit